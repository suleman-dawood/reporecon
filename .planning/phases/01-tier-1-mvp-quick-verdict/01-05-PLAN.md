---
phase: 01-tier-1-mvp-quick-verdict
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/staleness.sh
  - tests/golden/todo-cli.json
  - tests/golden/obscure-niche.json
  - tests/golden/llm-eval-dashboard.json
  - tests/run-goldens.sh
  - .github/workflows/goldens.yml
autonomous: true
requirements: [HEUR-02, HEUR-03, HEUR-04, SCR-05, TST-01]
must_haves:
  truths:
    - "scripts/staleness.sh emits badge tags (archived, stale-12mo, solo-stale-6mo) from candidate metadata JSON"
    - "scripts/staleness.sh works on both Linux (date -u -d) and macOS (date -j -f)"
    - "3 golden fixtures exist with {idea, expected_verdict_band} schema"
    - "tests/run-goldens.sh runs the skill non-interactively over each fixture (scaffold)"
    - ".github/workflows/goldens.yml installs gh ≥2.55 + jq + bash and runs run-goldens.sh on push"
  artifacts:
    - path: "scripts/staleness.sh"
      provides: "Mechanical staleness badge emitter (bash + jq + date)"
      contains: "stale-12mo"
    - path: "tests/golden/todo-cli.json"
      provides: "Saturated-domain golden fixture (expected 🔴)"
      contains: "expected_verdict_band"
    - path: "tests/golden/obscure-niche.json"
      provides: "Novel-domain golden fixture (expected 🟢)"
      contains: "expected_verdict_band"
    - path: "tests/golden/llm-eval-dashboard.json"
      provides: "Ambiguous-domain golden fixture (expected 🟡)"
      contains: "expected_verdict_band"
    - path: "tests/run-goldens.sh"
      provides: "Non-interactive smoke runner (scaffold; wiring in Plan 07)"
      contains: "expected_verdict_band"
    - path: ".github/workflows/goldens.yml"
      provides: "CI scaffold — installs deps + runs goldens runner"
      contains: "actions/checkout"
  key_links:
    - from: "scripts/staleness.sh"
      to: "verify-repo.sh output JSON"
      via: "Consumes metadata JSON (archived, pushed_at, contributor_count) on argv"
      pattern: "pushed_at"
    - from: ".github/workflows/goldens.yml"
      to: "tests/run-goldens.sh"
      via: "CI step runs the script"
      pattern: "run-goldens.sh"
---

<objective>
Create the mechanical staleness emitter, the 3 golden fixtures, the smoke-runner
scaffold, and the GitHub Actions workflow that ties them together. The runner is
a SCAFFOLD here — actual wiring against a live skill happens in Wave 3 (Plan 07)
after SKILL.md exists.

Purpose: Satisfies D-21, D-22, D-30, D-33, D-35 from CONTEXT.md; HEUR-02, HEUR-03,
HEUR-04, SCR-05, TST-01 from REQUIREMENTS.md. Staleness is the only mechanical
heuristic shipping in Tier 1 (vapor is Phase 2 per D-36).

Output: 6 files: 1 bash script + 3 JSON fixtures + 1 runner script + 1 workflow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tier-1-mvp-quick-verdict/01-CONTEXT.md
@.planning/phases/01-tier-1-mvp-quick-verdict/01-RESEARCH.md
@.planning/research/STACK.md

<interfaces>
scripts/staleness.sh <metadata-json-string>
  Input: a JSON string containing at minimum {archived, pushed_at, contributor_count}
         (this is what verify-repo.sh emits in Plan 02; metadata-json passed as $1)
  Output: space-separated badge tags on stdout, or empty string if no flags fire
  Exit 0 always (unless argv missing)
  Badges:
    archived            — when .archived == true
    stale-12mo          — when pushed_at is older than 365 days
    solo-stale-6mo      — when contributor_count == 1 AND pushed_at older than 180 days

Golden fixture JSON schema (one file per scenario):
  {
    "idea": "<the user's raw idea text>",
    "expected_verdict_band": "🔴" | "🟡" | "🟢",
    "scenario": "saturated" | "ambiguous" | "novel",
    "notes": "<why we expect this band>"
  }

tests/run-goldens.sh (SCAFFOLD)
  - For each tests/golden/*.json: prints scenario header, idea, expected band
  - Has a clearly marked TODO block where Plan 07 will wire the actual skill invocation
  - Exits 0 on scaffold mode; non-zero only on file-read errors

.github/workflows/goldens.yml
  - Triggers on push and pull_request
  - Ubuntu latest runner
  - Installs jq, gh ≥ 2.55 (official apt repo)
  - Requires GH_TOKEN secret for `gh auth login --with-token`
  - Runs tests/run-goldens.sh
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/staleness.sh — mechanical badge emitter</name>
  <files>scripts/staleness.sh</files>
  <read_first>
    - .planning/phases/01-tier-1-mvp-quick-verdict/01-CONTEXT.md (D-21, D-22, D-30)
    - .planning/phases/01-tier-1-mvp-quick-verdict/01-RESEARCH.md (Code Examples / Staleness badge emitter, Assumption A5 macOS date)
    - .planning/research/PITFALLS.md (HEUR-03: no auto-downgrade — script just emits flags)
  </read_first>
  <action>
    Create scripts/staleness.sh (mode 0755). Shebang `#!/usr/bin/env bash`, `set -euo pipefail`.

    Behavior:
    1. `meta="${1:?usage: staleness.sh <metadata-json>}"`
    2. Parse with jq:
       archived=$(echo "$meta" | jq -r '.archived')
       pushed_at=$(echo "$meta" | jq -r '.pushed_at')
       contributor_count=$(echo "$meta" | jq -r '.contributor_count // "null"')
    3. Convert pushed_at to epoch seconds with macOS fallback:
       pushed_s=$(date -u -d "$pushed_at" +%s 2>/dev/null || \
                  date -u -j -f "%Y-%m-%dT%H:%M:%SZ" "$pushed_at" +%s 2>/dev/null) || pushed_s=0
       now_s=$(date -u +%s)
       age_d=$(( (now_s - pushed_s) / 86400 ))
    4. Compose badges array:
       badges=()
       [[ "$archived" == "true" ]] && badges+=("archived")
       [[ "$age_d" -gt 365 ]] && badges+=("stale-12mo")
       if [[ "$contributor_count" =~ ^[0-9]+$ && "$contributor_count" -eq 1 && "$age_d" -gt 180 ]]; then
         badges+=("solo-stale-6mo")
       fi
    5. Output:
       echo "${badges[*]:-}"   # empty string if no badges

    Total length ≤45 lines.
  </action>
  <verify>
    <automated>bash -n scripts/staleness.sh && test -x scripts/staleness.sh && grep -q "stale-12mo" scripts/staleness.sh && grep -q "solo-stale-6mo" scripts/staleness.sh && grep -q "archived" scripts/staleness.sh && grep -q "set -euo pipefail" scripts/staleness.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/staleness.sh` passes
    - File executable
    - All three badges present as literal strings: archived, stale-12mo, solo-stale-6mo
    - macOS date fallback present (grep -q 'date -u -j -f' scripts/staleness.sh)
    - Smoke test 1 (archived): `bash scripts/staleness.sh '{"archived":true,"pushed_at":"2025-01-01T00:00:00Z","contributor_count":5}' | grep -q archived`
    - Smoke test 2 (active): `bash scripts/staleness.sh '{"archived":false,"pushed_at":"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'","contributor_count":10}'` outputs empty string
    - Smoke test 3 (solo-stale): `bash scripts/staleness.sh '{"archived":false,"pushed_at":"2024-01-01T00:00:00Z","contributor_count":1}'` outputs string containing `stale-12mo solo-stale-6mo` (both flags fire on this old solo repo)
  </acceptance_criteria>
  <done>Script syntax-valid, executable, all 3 smoke tests above pass.</done>
</task>

<task type="auto">
  <name>Task 2: 3 golden fixtures under tests/golden/</name>
  <files>tests/golden/todo-cli.json, tests/golden/obscure-niche.json, tests/golden/llm-eval-dashboard.json</files>
  <read_first>
    - .planning/phases/01-tier-1-mvp-quick-verdict/01-CONTEXT.md (D-33)
    - .planning/research/PITFALLS.md (UX pitfalls — golden test calibration)
  </read_first>
  <action>
    Create 3 JSON files, each containing:
      { "idea": "...", "expected_verdict_band": "...", "scenario": "...", "notes": "..." }

    tests/golden/todo-cli.json — saturated domain (expected 🔴):
      idea: "A CLI tool for managing personal todo lists in markdown files with tags and due dates"
      expected_verdict_band: "🔴"
      scenario: "saturated"
      notes: "Todo CLIs are a famously saturated GitHub niche. Should trigger at least one LIKELY_MATCH against repos like sindresorhus/todomvc-app-css, todotxt/todo.txt-cli, etc."

    tests/golden/obscure-niche.json — genuinely novel domain (expected 🟢):
      idea: "An NDIS invoice validator that checks Australian disability-services billing line items against current NDIS pricing arrangements and flags non-claimable items"
      expected_verdict_band: "🟢"
      scenario: "novel"
      notes: "NDIS-specific compliance tooling is genuinely sparse on GitHub. Devil's-advocate re-judge should still confirm 🟢. Tests proper-noun preservation (NDIS must not be paraphrased to 'disability scheme')."

    tests/golden/llm-eval-dashboard.json — ambiguous domain (expected 🟡):
      idea: "A web dashboard for evaluating LLM prompt performance across multiple models with side-by-side comparison and historical tracking"
      expected_verdict_band: "🟡"
      scenario: "ambiguous"
      notes: "LLM eval is hot but fragmented — partial overlaps with PromptFoo, OpenAI Evals, but rarely all features combined. Expected WORTH_INSPECTING band."

    Files must be valid JSON. No trailing commas.
  </action>
  <verify>
    <automated>jq -e '.idea and .expected_verdict_band and .scenario' tests/golden/todo-cli.json && jq -e '.idea and .expected_verdict_band and .scenario' tests/golden/obscure-niche.json && jq -e '.idea and .expected_verdict_band and .scenario' tests/golden/llm-eval-dashboard.json && [ "$(jq -r .expected_verdict_band tests/golden/todo-cli.json)" = "🔴" ] && [ "$(jq -r .expected_verdict_band tests/golden/obscure-niche.json)" = "🟢" ] && [ "$(jq -r .expected_verdict_band tests/golden/llm-eval-dashboard.json)" = "🟡" ]</automated>
  </verify>
  <acceptance_criteria>
    - 3 files exist at exact paths
    - All 3 parse as valid JSON
    - All 3 have keys: idea, expected_verdict_band, scenario, notes
    - Bands match: todo-cli=🔴, obscure-niche=🟢, llm-eval-dashboard=🟡
    - obscure-niche fixture's idea field contains the literal string "NDIS" (proper-noun guard test)
  </acceptance_criteria>
  <done>3 valid JSON fixtures present with correct expected bands.</done>
</task>

<task type="auto">
  <name>Task 3: tests/run-goldens.sh — non-interactive smoke runner (scaffold)</name>
  <files>tests/run-goldens.sh</files>
  <read_first>
    - .planning/phases/01-tier-1-mvp-quick-verdict/01-CONTEXT.md (D-35)
    - .planning/phases/01-tier-1-mvp-quick-verdict/01-RESEARCH.md (Open Question #5, Environment Availability)
  </read_first>
  <action>
    Create tests/run-goldens.sh (mode 0755). Shebang `#!/usr/bin/env bash`, `set -euo pipefail`.

    Behavior (SCAFFOLD — full skill invocation wired in Plan 07):
    1. Verify deps: gh, jq, bash version 4+. Print version table.
    2. Run scripts/preflight.sh (Plan 02 dependency); abort if exit non-zero.
    3. For each file in tests/golden/*.json:
       - Print "## Scenario: <scenario> — <fixture-filename>"
       - Print "Idea: <idea>"
       - Print "Expected band: <expected_verdict_band>"
       - SCAFFOLD MARKER block:
         echo "[TODO Plan 07] invoke skill non-interactively with idea, capture verdict, compare to expected band × 3 runs"
    4. Exit 0 on scaffold completion (it has not actually verified band-stability yet).

    Include a header comment block stating this is a Plan 05 scaffold, and Plan 07 (Wave 3) wires actual skill invocation + band-stability assertion (TST-02 ship gate).

    Total length ≤60 lines.
  </action>
  <verify>
    <automated>bash -n tests/run-goldens.sh && test -x tests/run-goldens.sh && grep -q "tests/golden" tests/run-goldens.sh && grep -q "TODO Plan 07" tests/run-goldens.sh && grep -q "expected_verdict_band" tests/run-goldens.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash -n passes
    - Executable
    - Iterates tests/golden/*.json
    - Reads expected_verdict_band from each
    - Contains explicit "TODO Plan 07" marker for the skill-invocation wiring
    - Smoke run: `bash tests/run-goldens.sh` exits 0 (assuming preflight passes locally)
  </acceptance_criteria>
  <done>Runner scaffold present, iterates fixtures, marked for Plan 07 wiring.</done>
</task>

<task type="auto">
  <name>Task 4: .github/workflows/goldens.yml — CI scaffold</name>
  <files>.github/workflows/goldens.yml</files>
  <read_first>
    - .planning/phases/01-tier-1-mvp-quick-verdict/01-CONTEXT.md (D-35)
    - .planning/phases/01-tier-1-mvp-quick-verdict/01-RESEARCH.md (Environment Availability table)
  </read_first>
  <action>
    Create .github/workflows/goldens.yml. Valid GitHub Actions YAML.

    Required structure:
      name: goldens
      on:
        push:
          branches: [main, master]
        pull_request:
      jobs:
        run:
          runs-on: ubuntu-latest
          steps:
            - uses: actions/checkout@v4
            - name: Install jq
              run: sudo apt-get update && sudo apt-get install -y jq
            - name: Install gh (>=2.55)
              run: |
                type -p curl >/dev/null || sudo apt-get install -y curl
                curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
                sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg
                echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list
                sudo apt-get update && sudo apt-get install -y gh
                gh --version
            - name: Authenticate gh
              env:
                GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: echo "$GH_TOKEN" | gh auth login --with-token
            - name: Run goldens
              run: bash tests/run-goldens.sh

    Use the built-in GITHUB_TOKEN (5000/hr authed); no extra secret required.

    Total length ≤45 lines.
  </action>
  <verify>
    <automated>test -f .github/workflows/goldens.yml && grep -q "actions/checkout" .github/workflows/goldens.yml && grep -q "run-goldens.sh" .github/workflows/goldens.yml && grep -q "gh --version" .github/workflows/goldens.yml && grep -q "GITHUB_TOKEN" .github/workflows/goldens.yml</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path
    - Valid YAML (parses with `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/goldens.yml'))"` or `yq . .github/workflows/goldens.yml`)
    - Contains: actions/checkout, jq install, gh install, run-goldens.sh invocation, GITHUB_TOKEN auth
  </acceptance_criteria>
  <done>Workflow file present, valid YAML, references run-goldens.sh and installs deps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metadata JSON → staleness.sh | Input is from verify-repo.sh (Plan 02), already gh-vetted |
| GH_TOKEN → CI | Built-in token, scoped to repo |
| golden fixtures → runner | Static JSON committed in repo; not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Malformed pushed_at crashes date parse | mitigate | `|| pushed_s=0` fallback; age_d then 0; no badges fire — fail safe |
| T-05-02 | Tampering | jq -r returns "null" when key missing | mitigate | contributor_count regex check `^[0-9]+$` before solo-stale gate |
| T-05-03 | Information Disclosure | GH_TOKEN leaked in CI logs | mitigate | Use GitHub-managed `secrets.GITHUB_TOKEN`; passed via env, not printf |
| T-05-04 | Denial of Service | Goldens workflow burns CI minutes on every push | accept | One job, no matrix, ≤2min runtime in Wave 1 scaffold |
</threat_model>

<verification>
- staleness.sh: 3 smoke tests pass
- 3 fixtures: valid JSON, correct expected bands
- run-goldens.sh: bash -n passes, lists all 3 fixtures, TODO marker for Plan 07
- goldens.yml: parses as valid YAML
- Files disjoint from all other Wave 1 plans
</verification>

<success_criteria>
6 files committed, all syntax-checks pass, staleness smoke tests pass, runner scaffold ready for Plan 07 to wire skill invocation.
</success_criteria>

<output>
Create `.planning/phases/01-tier-1-mvp-quick-verdict/01-05-SUMMARY.md` capturing:
- Staleness smoke-test outputs (all 3 scenarios)
- Fixture file list with expected bands
- CI workflow YAML validity check result
- Explicit handoff note to Plan 07: "Plan 07 must wire skill invocation + band-stability × 3 runs in tests/run-goldens.sh"
</output>

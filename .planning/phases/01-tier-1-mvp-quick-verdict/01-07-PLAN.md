---
phase: 01-tier-1-mvp-quick-verdict
plan: 07
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - tests/run-goldens.sh
  - skills/reporecon/SKILL.md
  - skills/reporecon/references/query-patterns.md
  - skills/reporecon/references/judge-rubric.md
  - skills/reporecon/references/report-template.md
autonomous: false
requirements: [T1-09, TST-01, TST-02, JDG-05, JDG-06]
must_haves:
  truths:
    - "tests/run-goldens.sh invokes the reporecon skill non-interactively against each golden fixture"
    - "Each fixture runs 3 consecutive times and the runner asserts the verdict band is identical across all 3 runs"
    - "Each fixture's verdict band matches its expected_verdict_band"
    - "Tier 1 wall-clock per run is ≤90 seconds on each golden fixture"
    - "Tunings (if any) to query-patterns.md / judge-rubric.md / report-template.md are committed and re-tested"
  artifacts:
    - path: "tests/run-goldens.sh"
      provides: "Full goldens runner — actual skill invocation + 3-run band-stability assertion"
      contains: "stability"
    - path: ".planning/phases/01-tier-1-mvp-quick-verdict/01-07-GOLDEN-RESULTS.md"
      provides: "Empirical results table: per fixture × per run → verdict band + wall-clock + axis sums"
      contains: "wall-clock"
  key_links:
    - from: "tests/run-goldens.sh"
      to: "skills/reporecon/SKILL.md"
      via: "Non-interactive Claude Code invocation (or simulated skill execution)"
      pattern: "SKILL.md"
    - from: "tests/run-goldens.sh"
      to: "./reporecon-reports/*.md"
      via: "Parses verdict badge from emitted report"
      pattern: "reporecon-reports"
---

<objective>
Validate Tier 1 ship gates: TST-02 (band stability × 3 runs per fixture) and T1-09
(≤90s wall-clock). Wire actual skill invocation into the runner scaffold from
Plan 05, run all 3 fixtures × 3 runs, and tune query patterns / judge rubric /
re-judge phrasing only as needed to lock band-stability.

Purpose: This is the Wave 3 calibration plan. Per RESEARCH.md Open Questions
1, 4, and Assumptions A6, A7 — these get resolved here empirically. Satisfies
T1-09, TST-02 (the ship gate per D-34), and validates the JDG-05 + JDG-06
mitigations on a real fixture.

This plan is NOT autonomous: it contains a human-verify checkpoint at the end
because golden-test calibration is iterative and the user must confirm the
ship gate is met before merging.

Output: Updated run-goldens.sh (real invocation), possibly tuned references,
plus a GOLDEN-RESULTS.md report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-tier-1-mvp-quick-verdict/01-CONTEXT.md
@.planning/phases/01-tier-1-mvp-quick-verdict/01-RESEARCH.md
@.planning/phases/01-tier-1-mvp-quick-verdict/01-01-SUMMARY.md
@.planning/phases/01-tier-1-mvp-quick-verdict/01-02-SUMMARY.md
@.planning/phases/01-tier-1-mvp-quick-verdict/01-03-SUMMARY.md
@.planning/phases/01-tier-1-mvp-quick-verdict/01-04-SUMMARY.md
@.planning/phases/01-tier-1-mvp-quick-verdict/01-05-SUMMARY.md
@.planning/phases/01-tier-1-mvp-quick-verdict/01-06-SUMMARY.md
@tests/run-goldens.sh
@skills/reporecon/SKILL.md
@skills/reporecon/references/query-patterns.md
@skills/reporecon/references/judge-rubric.md
@skills/reporecon/references/report-template.md

<interfaces>
Wired tests/run-goldens.sh contract:
  For each tests/golden/*.json:
    For run in 1..3:
      Invoke skill non-interactively with the fixture's `idea` field
        (use `claude` CLI in headless mode, or simulate by running the SKILL.md
         protocol manually via a wrapper script that executes the 7 steps)
      Parse the emitted report's H1 verdict badge (🟢 / 🟡 / 🔴)
      Record: run #, verdict, wall-clock seconds, axis_sum totals
    Assert: all 3 runs produced the same band
    Assert: band matches fixture's expected_verdict_band
    Assert: wall-clock for each run ≤ 90s
  Exit 0 only if ALL fixtures × ALL runs pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire tests/run-goldens.sh — real skill invocation + 3-run stability + timing</name>
  <files>tests/run-goldens.sh</files>
  <read_first>
    - tests/run-goldens.sh (Plan 05 scaffold — TODO Plan 07 markers)
    - skills/reporecon/SKILL.md (Plan 06 — what gets invoked)
    - All 3 fixtures under tests/golden/
    - .planning/phases/01-tier-1-mvp-quick-verdict/01-RESEARCH.md (Assumption A7 — wall-clock budget)
  </read_first>
  <action>
    Replace the Plan 05 TODO scaffold with real wiring. Required behavior:

    1. For each tests/golden/*.json fixture:
       For run in 1 2 3:
         a) Record start_ts (`date -u +%s`)
         b) Invoke the skill. Two modes (auto-detect):
            - If `claude` CLI is available with non-interactive flag (e.g.,
              `claude --headless --skill reporecon "$IDEA"`): use that
            - Otherwise (CI fallback): the runner emits a structured fixture
              prompt to stdout and expects the test job to be invoked by a
              human in Claude Code, OR the runner stubs by running each
              SKILL.md step manually using the bash scripts + a constrained
              local LLM mock. For Phase 1, **default to the claude CLI path**
              and document the fallback in the script's header.
         c) Record end_ts; compute wall_clock=(end_ts - start_ts)
         d) Find the latest ./reporecon-reports/*.md file (sorted by mtime).
            Parse the H1 line. Extract emoji (🟢, 🟡, 🔴) into `band`.
            Parse the per-candidate axis_sum lines into `total_axis_sum`.
         e) Append CSV row to a per-fixture results file:
              fixture,run,band,wall_clock,total_axis_sum

    2. After 3 runs of one fixture:
       - Read all 3 bands. If they are not identical → record FAIL (band
         instability). Print a "stability" failure line.
       - If band != fixture's expected_verdict_band → FAIL (wrong band).
       - If any wall_clock > 90 → FAIL (T1-09 budget).

    3. After all fixtures:
       Print summary table (fixture × run × band × wall_clock).
       Exit 0 only if all 9 cells (3 fixtures × 3 runs) PASS all 3 checks.
       Otherwise exit non-zero with a one-line summary of which checks failed.

    4. Keep the script ≤120 lines. Use jq for fixture parsing and a simple
       awk/grep for report parsing.

    The header comment block must explicitly call out:
    - This script is the TST-02 ship gate
    - The 90s budget is T1-09
    - Calibration step: if a fixture fails, tune query-patterns.md / judge-rubric.md
      and re-run; commit tunings as separate commits within this plan
  </action>
  <verify>
    <automated>bash -n tests/run-goldens.sh && test -x tests/run-goldens.sh && grep -q "stability" tests/run-goldens.sh && grep -q "expected_verdict_band" tests/run-goldens.sh && grep -q "90" tests/run-goldens.sh && ! grep -q "TODO Plan 07" tests/run-goldens.sh</automated>
  </verify>
  <acceptance_criteria>
    - bash -n passes, file executable
    - Contains "stability" check
    - Contains 90s budget check
    - "TODO Plan 07" marker REMOVED
    - References all 3 fixtures and runs each 3 times
    - Exits 0 iff every fixture × every run passes all 3 assertions
  </acceptance_criteria>
  <done>Runner wired; ready to run for real against the skill.</done>
</task>

<task type="auto">
  <name>Task 2: Run goldens × 3, tune as needed, record empirical results</name>
  <files>
    .planning/phases/01-tier-1-mvp-quick-verdict/01-07-GOLDEN-RESULTS.md,
    skills/reporecon/references/query-patterns.md,
    skills/reporecon/references/judge-rubric.md,
    skills/reporecon/references/report-template.md,
    skills/reporecon/SKILL.md
  </files>
  <read_first>
    - tests/run-goldens.sh (Task 1)
    - All 3 reference files + SKILL.md (current state from Wave 1+2)
  </read_first>
  <action>
    Execute the runner. If FAILs occur, tune in this priority order (smallest
    change first):
    1. **Re-judge prompt phrasing** (Open Question #1): try the two phrasings
       from RESEARCH.md ("argue this idea already exists" vs "list the strongest
       case that this candidate IS the user's idea") — pick the one that
       stabilizes obscure-niche at 🟢 across 3 runs.
    2. **Devil's-advocate trigger threshold** (Assumption A6): if obscure-niche
       flip-flops 🟢↔🟡, narrow the trigger condition (e.g., raise the axis≥2
       gate to axis≥3, or cap re-judges to candidates with axis_sum≥6).
    3. **Query archetype phrasing** (query-patterns.md): if todo-cli does not
       hit 🔴 reliably, tighten the LITERAL archetype example.
    4. **Threshold table** (judge-rubric.md): only if the above don't suffice,
       adjust the core_pair/axis_sum thresholds — but document the
       calibration rationale.
    5. **Wall-clock** (Assumption A7): if any fixture blows 90s, reduce
       candidate count from 5 to 4 in SKILL.md Step 4 (Dedup + Rank);
       alternatively reduce per-search sleep from 300ms → 100ms (Pitfall 6
       tolerance).

    After each tuning, re-run the entire goldens suite (3 fixtures × 3 runs).
    Commit each tuning as a separate commit with message
    `tune(01-07): <what changed> — <fixture> went from <before> to <after>`.

    When all 9 cells PASS, write
    `.planning/phases/01-tier-1-mvp-quick-verdict/01-07-GOLDEN-RESULTS.md`
    containing:
    - A markdown table: fixture × run × band × wall_clock × axis_sum × pass/fail
    - A "Tunings applied" section listing every reference-file change made in
      this plan, with a one-line rationale each
    - A "Phase 1 ship gate status" line: PASS or FAIL
  </action>
  <verify>
    <automated>test -f .planning/phases/01-tier-1-mvp-quick-verdict/01-07-GOLDEN-RESULTS.md && grep -q "PASS" .planning/phases/01-tier-1-mvp-quick-verdict/01-07-GOLDEN-RESULTS.md && bash tests/run-goldens.sh</automated>
  </verify>
  <acceptance_criteria>
    - GOLDEN-RESULTS.md exists and lists 9 cells (3 fixtures × 3 runs) with band, wall_clock, pass/fail
    - All 9 cells show "PASS" for band-match, stability, and ≤90s budget
    - Final `bash tests/run-goldens.sh` invocation exits 0
    - Every tuning commit in the git log for this plan has the `tune(01-07):` prefix
  </acceptance_criteria>
  <done>All 9 cells pass; ship gate documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify Phase 1 ship gate</name>
  <what-built>
    Tier 1 MVP: installable plugin returns 🟢/🟡/🔴 verdict via /reporecon, all 3 goldens
    produce stable band across 3 runs, all ≤90s, no hallucinated URLs, mechanical
    5-axis rubric with derived verdicts.
  </what-built>
  <how-to-verify>
    1. Open .planning/phases/01-tier-1-mvp-quick-verdict/01-07-GOLDEN-RESULTS.md
       → verify all 9 cells show PASS
    2. Run `bash tests/run-goldens.sh` in a clean shell → exits 0
    3. Spot-check one fresh manual run: `/reporecon "<some idea you care about>"`
       → verdict block appears in chat in <90s, report file written under ./reporecon-reports/
    4. Confirm none of the deferred features (clones, WebSearch, file-path evidence, vapor heuristic, EXACT/PARTIAL verdicts) have leaked into Phase 1 output
    5. Inspect the latest report file → header has sharpened statement + preserved terms; every candidate URL has "verified at <ISO timestamp>"; footer shows Tier 2 deferral
  </how-to-verify>
  <resume-signal>Type "approved" to lock Phase 1, or describe issues to send back for additional tuning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture idea → skill | Static fixture text; same trust as user input |
| skill output → runner parser | Runner reads report file written by skill — trusted local artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Tuning loop introduces calibration that fits goldens but fails real ideas | mitigate | Tunings limited to phrasing + thresholds; not to fixture text; human-verify checkpoint requires a manual run on a real idea |
| T-07-02 | Repudiation | Failure cause unclear from runner output | mitigate | Runner emits explicit failure-line per assertion (stability vs band-mismatch vs budget) |
| T-07-03 | Information Disclosure | Reports from goldens contain real GitHub data | accept | Public repos only; reports gitignored by default per Plan 01 .gitignore |
</threat_model>

<verification>
- run-goldens.sh: real invocation wired, "TODO Plan 07" removed
- GOLDEN-RESULTS.md: 9 cells, all PASS
- `bash tests/run-goldens.sh` exits 0
- Human-verify checkpoint approved
- This plan is the only one in Wave 3 — no parallel-file conflicts to worry about
</verification>

<success_criteria>
Phase 1 ship gate locked: 3 fixtures × 3 runs all produce expected band, all ≤90s, runner exits 0, human approved.
</success_criteria>

<output>
Create `.planning/phases/01-tier-1-mvp-quick-verdict/01-07-SUMMARY.md` capturing:
- Final 9-cell results table
- List of tunings applied (or "none required")
- Resolutions for Open Questions 1, 4 and Assumptions A6, A7
- Phase 1 ship-gate status: PASS
- Handoff notes for Phase 2 (e.g., observed cases where Tier 1 was 🟡 → user would want Tier 2)
</output>
